# Work Log - 2026-01-09

- Started server run with iterator mutable threshold enabled: `TREEDB_ITERATOR_MUTABLE_MAX_BYTES=4194304`.
- Launched `nohup ./run_celestia.sh` on server; PID `1239177` and log `~/celestia_trace_run.log`.
- Added memtable mode benchmark matrix (`BenchmarkTraceReplayMemtableModes`) and ran locally with trace replay. Results (Apple M3, scale=1.0): adaptive ~89.8ms/op, skiplist ~62.3ms/op, hash_sorted ~34.4ms/op, btree ~38.2ms/op. Indicates hash_sorted best for this trace; btree faster than skiplist but slower than hash_sorted.
- Server trace run (PID `1239177`) completed successfully; captured `/home/mikers/treedb_trace_20260109064203.jsonl` and summarized to `/home/mikers/treedb_trace_20260109064203.summary.json`.
- Pulled summary to local as `tmp_trace_summary_20260109064203.json` and reran replay bench (scale=1.0): default ~70.7ms/op; adaptive ~69.4ms/op, skiplist ~66.4ms/op, hash_sorted ~45.9ms/op, btree ~54.2ms/op.
- Added timeline replay benchmark and trace capture helpers; pulled `/home/mikers/treedb_trace_20260109064203.jsonl` and ran `BenchmarkTraceReplayTimeline` with `TREEDB_TRACE_TIMELINE_DURATION_MS=1000` (~3.15s/op).
- Note: initial trace capture attempt failed due to local `~/run_celestia.sh` expansion; rerun uses `/home/mikers/run_celestia.sh`.
- New server trace run completed (`/home/mikers/.celestia-app-mainnet-treedb-20260109071235`), duration 446s per `sync-time.log`.
- Pulled `/home/mikers/treedb_trace_20260109071235.jsonl` and generated summary; timeline replay with `TREEDB_TRACE_TIMELINE_DURATION_MS=1000` -> ~3.46s/op (~129x faster than wall clock).
- Timeline replay scaling sweep (same trace): 3000ms -> ~9.06s/op, 5000ms -> ~15.1s/op, 10000ms -> ~30.1s/op. 3000–5000ms seems like a good balance.
- Timeline replay memtable modes at 10000ms: adaptive ~30.23s/op, skiplist ~30.21s/op, hash_sorted ~30.17s/op, btree ~30.30s/op.
- Profile-based optimization priorities (server trace run `20260109083900`): focus on TreeDB zipper merge/write path and leaf key decoding (`zipper.writeRecursive`, `mergeLeaf`, `node.leafEntryKeyAt`, `GetLeafEntryView`), then flush/build path (`flushCombinedLocked`, `Batch.SetOps`), GC pressure (`runtime.scanobject`), and slab append (`SlabManager.AppendMany`). Mutex/block profiles were empty, so contention not visible in this run.
- Added timeline replay knobs to reduce scheduler noise and focus write path: `TREEDB_TRACE_TIMELINE_NO_SLEEP`, `TREEDB_TRACE_TIMELINE_INLINE_ITERS`, `TREEDB_TRACE_SKIP_ITERS`. Profile now shows `zipper.Apply` and batch write paths, plus sorting overhead.
- Added sequential key mode and batch assume-sorted support for benchmarking; backend-mode profile now highlights zipper merge/write paths (`writeRecursive`, `mergeLeaf`, `AddLeafEntry`) similar to Celestia hotspots.
- Enabled leaf prefix compression in the trusted benchmark and added `mergeLeaf` old-entry caching to avoid double `GetLeafEntryView`; prefix-compressed benchmark improved (~771ms/op -> ~719ms/op) and now shows `leafEntryKeyAt` as a top hotspot (matching Celestia).
- Added a sequential cache fast path in `node.leafEntryKeyAt` for prefix-compressed scans; trusted benchmark improved again (~719ms/op -> ~704ms/op) and `leafEntryKeyAt` flat time dropped (~16.6s -> ~15.0s).
- Added `LeafEntrySizeWithPrefix` + `AddLeafEntryWithPrefix` and used them in `zipper.mergeLeaf` to avoid recomputing prefix/suffix on every insert; trusted benchmark improved (~704ms/op -> ~664ms/op).
- Tried to skip redundant prefix copies in `leafEntryKeyAt` when the scratch buffer is reused; benchmark is within noise (~666ms/op, ~663ms/op). Collapsing prefix writes into a single `PutUint32` regressed and was reverted.
- Started a traced Celestia run on the server: `/home/mikers/run_celestia_trace.sh` (PID 1373309), trace dir `/home/mikers/pprof_20260109120107`. CPU pprof shows TreeDB leaf key decode and zipper paths as expected.
- Pulled trace `treedb_trace_20260109120107.jsonl`, generated summary with `cmd/trace_bench`, and ran trusted timeline replay: ~642ms/op (backend + sequential keys + prefix compression, 5000ms).
- Tried a compare-only path in `searchLeafPrefixCompressed` to avoid reconstructing full keys; benchmark regressed (~668ms/op) and the change was reverted.
- Tried caching restart keys in `leafEntryKeyAt` to reduce prefix decode work; results were within noise (~644–646ms/op vs ~642ms/op) and the change was reverted.
- Tried hoisting `n.data`/`len(n.data)` locals in `leafEntryLayoutAt`/`leafEntryKeyAt`; results were noisy (628/648/688ms/op) and the change was reverted.
- Tried flushing `SetOps` per-memtable with sorted/unsorted hints; benchmark regressed (~675–699ms/op) and the change was reverted.
- Tried reusing `slab.AppendMany` scratch slices to reduce allocations; results were noisy/regressive (646/716ms/op) and the change was reverted.
- Added a `mergeInternal` childWork slice pool; trusted benchmark improved slightly (~637–639ms/op vs ~642ms/op baseline).
- Tried consolidating `slab.AppendMany` compression flags into prep entries to remove bool slices; benchmark regressed (~680–707ms/op) and was reverted.
- Added shortest separator keys for leaf splits; trusted benchmark improved (~622–627ms/op).
- Tried shortest separator keys for internal splits; benchmark regressed (~692ms/op) and the change was reverted.
- Reproduced CI regression from `caching: recycle memtables with reader tracking` (wrong values + missing backend keys in `TestCachingDB_WriteAndFlush` and `TestUnsafeOptions_ConcurrentStress`). Disabling memtable pooling fixes the corruption; tests pass with `-count=50` and `-count=10` respectively.
- Bench check: cached timeline replay w/ skiplist shows pooling enabled ~762ms/op vs disabled ~754ms/op, so no clear perf win to offset risk.
- Fully removed memtable pooling/reader tracking code; reran caching regression tests (`TestCachingDB_WriteAndFlush -count=20`, `TestUnsafeOptions_ConcurrentStress -count=5`) to confirm clean.
- Pulled trace `treedb_trace_20260109120107.jsonl` from server and generated summary locally via `cmd/trace_bench`.
- Ran cached timeline replay matrix across leaf prefix compression, slab compression (none/zstd), and force value pointers:
  - leaf=0 slab=none forceptr=0: ~660ms/op
  - leaf=0 slab=none forceptr=1: ~694ms/op
  - leaf=0 slab=zstd forceptr=0: ~651ms/op
  - leaf=0 slab=zstd forceptr=1: ~705ms/op
  - leaf=1 slab=none forceptr=0: ~700ms/op
  - leaf=1 slab=none forceptr=1: ~743ms/op
  - leaf=1 slab=zstd forceptr=0: ~810ms/op
  - leaf=1 slab=zstd forceptr=1: ~727ms/op
  - Observed leaf prefix compression + force pointers regress this replay; zstd slab only slightly helps when both are off.
- Updated server `~/run_celestia.sh` to match fastest replay config: `TREEDB_LEAF_PREFIX_COMPRESSION=0`, `TREEDB_FORCE_VALUE_POINTERS=0` (kept slab zstd).
- Ran A/B benchmarks for other server flags (cached timeline replay, 5s):
  - baseline (`DISABLE_VLOG=1`, `SLAB_OMIT_KEYS=1`, `ENABLE_VALUE_INDEX=1`, `ITERATOR_MUTABLE_MAX_BYTES=4194304`, `FLUSH_THRESHOLD=33554432`): ~632ms/op
  - value log enabled: ~631ms/op
  - slab omit keys off: ~655ms/op
  - value index off: ~636ms/op
  - iterator mutable max bytes off: ~639ms/op
  - flush threshold 16MB: ~659ms/op
- Started server param sweep via `/home/mikers/run_celestia_param_sweep.sh` (8 runs, 5 minutes each) logging to `/home/mikers/treedb_param_sweep.log`.
- Param sweep script initially failed (bad `tr`/`awk` quoting); fixed and restarted `run_celestia_param_sweep.sh`.
- Param sweep crashed again due to `du` on missing slab files; updated script to guard empty slab glob and restarted.
- Param sweep crashed again due to broken awk quoting (missing braces). Fixed awk quoting and restarted.
- Param sweep completed (8 runs × 5 minutes each). Extracted true sizes from each run's `sync/disk-breakdown.log` (the sweep script's `index_bytes/slab_bytes` were 0 due to a path mismatch). Results (duration_sec from `sync/sync-time.log`, `index_bytes` + `slab_bytes` from disk breakdown):
  - `baseline`: `duration_sec=225`, `index_bytes=5435817984`, `slab_bytes=457656922` (`~.celestia-app-mainnet-treedb-20260109200128`)
  - `leaf_prefix_1`: `duration_sec=225`, `index_bytes=5435817984`, `slab_bytes=459698510` (`~...20260109200633`)
  - `force_ptr_1`: `duration_sec=236`, `index_bytes=5435817984`, `slab_bytes=462207905` (`~...20260109201138`)
  - `value_index_0`: `duration_sec=204`, `index_bytes=5234491392`, `slab_bytes=421545746` (`~...20260109201643`) — fastest and smaller index
  - `slab_compression_none`: `duration_sec=235`, `index_bytes=5502926848`, `slab_bytes=466206035` (`~...20260109202148`) — worse than zstd here
  - `slab_omit_keys_0`: `duration_sec=204`, `index_bytes=5301600256`, `slab_bytes=425495056` (`~...20260109202653`) — faster, index smaller than baseline but larger than `value_index_0`
  - `best_fastest` (as guessed): `duration_sec=236`, `index_bytes=5502926848`, `slab_bytes=470416691` (`~...20260109203158`) — not best
  - `best_tradeoff` (as guessed): `duration_sec=214`, `index_bytes=5301600256`, `slab_bytes=430130818` (`~...20260109203703`) — fastest but not smallest index
- Next: re-run a tighter follow-up (short list) around the apparent winners (`value_index_0`, `slab_omit_keys_0`, and maybe `leaf_prefix`/`force_ptr` combos) to reduce variance and confirm.
- Bench check: tried `SetView`/`DeleteView` fast path in flush, but trusted trace benchmark regressed (~423ms/op baseline vs ~485ms/op with change). Dropped the change.
- Ran `run_celestia_trace.sh` with inline values (`TREEDB_FORCE_VALUE_POINTERS=0`) and captured pprof at `/home/mikers/pprof_20260109230253`.
  - Sync summary: duration 257s; index 4.43G, slab 0.45G; shutdown clean.
  - CPU hotspots: `node.putUint16`, `node.(*Node).leafEntryKeyAt`, `node.(*Builder).AddLeafEntryWithPrefix`, GC (`runtime.scanobject`), `runtime.memmove`.
- Bench check (local timeline replay): replaced `binary.LittleEndian.Uint16` with `getUint16` in leaf/node paths; average ~4% improvement across 3 runs; kept.
- Bench check (local timeline replay): replaced `binary.LittleEndian.PutUint32` with `putUint32` in builder/leaf paths; baseline ~513ms/op vs ~454ms/op (avg of 3 runs); kept.

## TODO
- Evaluate whether `TREEDB_ITERATOR_MUTABLE_MAX_BYTES` should stay opt-in or become a safe default. Context: this option allows iterators to read from mutable memtables without forcing a rotation when mutable bytes are small, improving perf but potentially increasing writer blocking while iterators are open. Use the server run results (PID `1239177`, log `~/celestia_trace_run.log`) and trace/bench outputs to judge safety and performance tradeoffs.
