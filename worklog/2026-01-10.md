# Work Log - 2026-01-10

- Confirmed the 8 param sweep run directories still exist on server: `/home/mikers/.celestia-app-mainnet-treedb-2026010920*`.
- Ran offline index vacuum on the baseline run’s application DB (`/home/mikers/.celestia-app-mainnet-treedb-20260109200128/data/application.db`) using:
  - `PATH=/home/mikers/go1.23.4/bin:$PATH GOWORK=off go run ./TreeDB/cmd/treemap vacuum <db-dir> -chunk-size-mib 64`
- Result: vacuum rewrote `index.db` (mtime updated) but did not reduce size:
  - before: `index.db` bytes = `5435817984`
  - after:  `index.db` bytes = `5435817984`
- Fragmentation report (before vs after) was effectively unchanged (leaf fill ~98.5%, internal fill ~99.5%), suggesting the large `index.db` size is mostly “true data size”, not fragmentation/ballooning in this dataset.
- Server run `~/.celestia-app-mainnet-treedb-20260109215403` review:
  - `duration_seconds=425` (sync-time log), `final_local_height=9323113`.
  - `index.db` bytes: `2550136832`; slabs total: `4056772808` (from disk-breakdown/top files).
  - Config (from run_celestia.sh at that time): `TREEDB_LEAF_PREFIX_COMPRESSION=1`, `TREEDB_DISABLE_VALUE_LOG=1`, `TREEDB_MEMTABLE_VALUE_LOG_POINTERS=1`, `TREEDB_SLAB_COMPRESSION=zstd`, `TREEDB_ENABLE_VALUE_INDEX=1`, `TREEDB_FORCE_VALUE_POINTERS=1`, `TREEDB_VALUELOG_POINTER_THRESHOLD=32`, `TREEDB_BENCH_DISABLE_BG=0`, `TREEDB_ITERATOR_MUTABLE_MAX_BYTES=4194304`, `TREEDB_FLUSH_THRESHOLD=33554432`, `TREEDB_BACKGROUND_COMPACTION_INTERVAL=1m`, `TREEDB_CLOSE_CHECKPOINT=1`, `TREEDB_CLOSE_VACUUM_INDEX_ONLINE=1`, `TREEDB_CLOSE_VACUUM_TIMEOUT=45m`, `TREEDB_CLOSE_SCOPE_CONTAINS=application.db`, `TREEDB_CLOSE_LOG=1`, `TREEDB_SLAB_OMIT_KEYS=1`.
- Server run `~/.celestia-app-mainnet-treedb-20260109220904` (user run):
  - `duration_seconds=300`, `final_local_height=9323245`.
  - `index.db` bytes: `1744830464`; slabs total: `4201310833`. `index.db.new` present (`201326592` bytes).
  - Config used (from `~/run_celestia.sh`): `TREEDB_LEAF_PREFIX_COMPRESSION=1`, `TREEDB_DISABLE_VALUE_LOG=1`, `TREEDB_MEMTABLE_VALUE_LOG_POINTERS=1`, `TREEDB_SLAB_COMPRESSION=zstd`, `TREEDB_ENABLE_VALUE_INDEX=0`, `TREEDB_FORCE_VALUE_POINTERS=1`, `TREEDB_VALUELOG_POINTER_THRESHOLD=32`, `TREEDB_BENCH_DISABLE_BG=0`, `TREEDB_ITERATOR_MUTABLE_MAX_BYTES=4194304`, `TREEDB_FLUSH_THRESHOLD=33554432`, `TREEDB_BACKGROUND_COMPACTION_INTERVAL=1m`, `TREEDB_CLOSE_CHECKPOINT=1`, `TREEDB_CLOSE_VACUUM_INDEX_ONLINE=1`, `TREEDB_CLOSE_VACUUM_TIMEOUT=45m`, `TREEDB_CLOSE_SCOPE_CONTAINS=application.db`, `TREEDB_CLOSE_LOG=1`, `TREEDB_SLAB_OMIT_KEYS=1`.
- Crash analysis for `~/.celestia-app-mainnet-treedb-20260109220904`:
  - `sync/node.log` shows a panic during shutdown: `panic: treedb: db is closed` from `cometbft/store.(*BlockStore).LoadSeenCommit` while a consensus reactor goroutine was still running.
  - Shutdown sequence in `mainnet-treedb-fast-sync-forensics.sh` sends `SIGINT` and waits; the panic occurs after `Caught up. Stopping node...`, while DBs are closing.
  - Likely root cause: CometBFT shutdown ordering (blockstore closed while consensus goroutine still running). Not a TreeDB data corruption issue, but a shutdown race that causes a panic on `ErrClosed`.
- Implemented a TreeDB adapter mitigation: when the DB is closed, `Get/GetUnsafe/GetAppend/Has` now return nil/false without propagating `ErrClosed` so CometBFT does not panic during shutdown. Added regression test in `kvstore/adapters/treedb/treedb_close_test.go`.
- Updated server build inputs to compile with the adapter change:
  - `celestia-app/app/app.go`: switched `treedbadapter.SetTracePhase` calls to `treedbtrace.SetTracePhase`.
  - `go.work` (in `/home/mikers/dev/snissn`) and `celestia-app/go.mod` set to `go 1.25.5` for toolchain consistency; build performed via `go build -o build/celestia-appd ./cmd/celestia-appd` with `GOTOOLCHAIN=go1.25.5` (skipped `make build-standalone` due to submodule `go mod tidy` mismatch).
- Verified shutdown panic resolved: server run `~/.celestia-app-mainnet-treedb-20260109223212` completed and `sync/node.log` contains no `panic`/`db is closed` entries; close vacuum completed successfully.
- Collected server CPU/heap/goroutine profiles during run (pprof via `localhost:6062`, 30s CPU). Top CPU hotspots included `runtime.memmove`, `runtime.scanobject`, `github.com/klauspost/compress/zstd.(*fastEncoder).EncodeNoHist`, `node.(*Node).leafEntryKeyAt`, and `node.(*Builder).AddLeafEntryWithPrefix` / `encoding/binary.littleEndian.PutUint16`.
- Low-risk optimization: replaced hot `binary.LittleEndian.PutUint16` calls in `TreeDB/node/builder.go` with a local `putUint16` helper in leaf/internal builder paths.
  - Local replay bench (timeline, 5s) improved from ~664ms/op to ~644ms/op on the same trace (~3% faster).
- Second micro-optimization: when leaf prefix compression is enabled and `prefixLen==0`, `leafEntryKeyAt` now returns a direct key view into node data instead of copying into the scratch buffer (fast path + restart path).
  - Local replay bench improved to ~616ms/op on the same trace (additional ~4%).
- Server config updated for inline values: `TREEDB_FORCE_VALUE_POINTERS=0` and `run_celestia.sh` now builds via `go build` with `GOTOOLCHAIN=go1.25.5` (avoids `make build-standalone` failures).
- Started inline-values server run: `nohup ~/run_celestia.sh > ~/celestia_inline_run.log 2>&1 &` (PID `1637321`, run dir `~/.celestia-app-mainnet-treedb-20260109224518`).
- Inline-values pprof capture: `/home/mikers/pprof_cpu_inline_20260109224548.prof` (30s CPU), heap `/home/mikers/pprof_heap_inline_20260109224548.prof`, goroutine `/home/mikers/pprof_gor_inline_20260109224548.prof`. Top CPU shows `node.putUint16`, `leafEntryKeyAt`, `AddLeafEntryWithPrefix`, GC, and zstd encode.
- Tried two additional micro-optimizations (packed prefix/suffix as uint32; skip valLen decode for pointer entries); both regressed local replay and were reverted.
- Inline-values run completed without `panic`/`db is closed`:
  - `duration_seconds=278`, `final_local_height=9323591`.
  - `index.db` bytes: `4294967296`; slabs total: `462926808`.
