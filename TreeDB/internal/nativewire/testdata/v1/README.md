# Native Wire v1 Fixtures

These fixtures pin the initial R0 codec bytes for the internal native-wire
package. They are intentionally small and should grow alongside command schemas,
negative conformance tests, and deterministic-entry coverage.

