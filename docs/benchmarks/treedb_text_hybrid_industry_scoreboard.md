# TreeDB text-v2/hybrid industry comparison scoreboard

This runbook has two deliberately separate outputs:

1. the existing TreeDB text/vector/hybrid Go-benchmark scoreboard; and
2. the pinned, same-corpus lexical comparison introduced by #4330.

The lexical comparison is the only artifact suitable for cross-engine lexical
latency, build, storage, and result-equivalence tables. It is benchmark evidence,
not a timing assertion or a relevance-quality claim.

## One retained run

Run the lexical comparison directly when hybrid rows are not needed:

```sh
RUN_DIR=/tmp/gomap_lexical_compare_$(date +%Y%m%d_%H%M%S)
python3 benchmarks/text_hybrid_scoreboard/run_lexical_comparison.py \
  --manifest benchmarks/text_hybrid_scoreboard/lexical_manifest.json \
  --out-dir "$RUN_DIR" \
  --repetitions 3 \
  --timeout-seconds 900
```

The runner rejects fewer than three retained repetitions and a dirty checkout.
It records the exact commit, source tree, TreeDB subtree, comparator-harness
subtree, and dirty state; the TreeDB raw artifacts are bound to that commit.
`--allow-dirty` is development-smoke-only and marks the consolidated report
ineligible for retained qualification. The runner prepares pinned external
dependencies, then runs engines serially in this order: TreeDB text-v2, Apache
Lucene, Bleve, SQLite FTS5. Every repetition creates, checkpoints, closes,
reopens, validates, and then removes its own index unless `--keep-indexes` is
specified.

The established combined entry point also runs this comparison by default:

```sh
RUN_DIR=/tmp/gomap_text_hybrid_scoreboard_$(date +%Y%m%d_%H%M%S) \
  scripts/bench_text_hybrid_scoreboard.sh
```

Set `RUN_LEXICAL_COMPARISON=false` only to reproduce the legacy hybrid-only
matrix. `LEXICAL_REPETITIONS` defaults to `3` and cannot be less than `3`.

## Frozen logical corpus and queries

`benchmarks/text_hybrid_scoreboard/lexical_manifest.json` is the versioned
contract. `lexical_common.py/frozen_corpus_v1` materializes its canonical UTF-8
TSV corpus. The runner checks the generated bytes against the pinned SHA-256
before any engine starts. Every adapter reads those same bytes and reports its
own document count and corpus digest; the reference validator rejects drift.

The frozen contract is:

- 10,000 deterministic documents with `id`, `title`, `body`, and `tenant`;
- Unicode NFKC plus lowercase normalization and contiguous ASCII alphanumeric
  tokens, with no stopwords;
- `title` weight 3, `body` weight 1, BM25 `k1=1.2`, `b=0.75`;
- top-K 10 with score-descending/document-ID-ascending ties;
- two warmups and 20 retained raw latency samples per query per repetition;
- warm in-process cache state after build and reopen;
- common term, rare term, AND, OR, exact phrase, and scalar-filtered term cases.

The fixture intentionally makes the expected top-K IDs stable across the
explicit analyzers and BM25 implementations. This validates execution and
result equivalence; it does not claim that engine scores are numerically
identical or that the synthetic fixture measures production relevance.

## Pinned engines and setup

| engine | pin | analyzer/index configuration | setup command |
| --- | --- | --- | --- |
| TreeDB | checked-out root module/commit | accepted text-v2, simple analyzer, stored positions, score-only, no document fetch | root Go module |
| Apache Lucene | `org.apache.lucene:lucene-core:9.12.1`, `lucene-analysis-common:9.12.1` | `StandardAnalyzer`, `BM25Similarity(1.2,0.75)`, FSDirectory | `cd benchmarks/text_hybrid_scoreboard/lucene_adapter && mvn -q -DskipTests dependency:go-offline` |
| Bleve | `github.com/blevesearch/bleve/v2 v2.4.4` | standard analyzer, keyword tenant, Scorch | `cd benchmarks/text_hybrid_scoreboard/bleve_adapter && GOWORK=off go mod tidy` |
| SQLite FTS5 | Python stdlib SQLite build, exact Python/SQLite versions captured per artifact | `unicode61 remove_diacritics 2`, `bm25(0,3,1,0)`, WAL + FULL synchronous | no downloaded dependency |

The Lucene Maven project and Bleve Go module are copied into isolated dependency
workspaces under the run directory; neither changes the checkout or root Go
module. Maven plugins, Jackson, Lucene, and Bleve are all exact-version pins.

Missing `go`, `java`, or `mvn`, a failed pinned dependency setup, or a Python
SQLite build without FTS5 becomes a typed `unavailable` artifact with the exact
setup command, reason, and bounded stderr. An adapter execution error after
successful setup is not relabeled as unavailable and aborts the run. Timeout,
fallback, or partial output also aborts rather than producing a row.

## Shared result and validation contract

Each repetition emits `treedb_lexical_result/v1`, formally described by
`benchmarks/text_hybrid_scoreboard/lexical_result.schema.json`. An accepted engine artifact contains:

- exact command, engine/runtime versions, and complete analyzer/index config;
- manifest and corpus digests plus document count;
- build elapsed time, throughput, CPU time, and peak RSS where the runtime can
  report it;
- checkpointed durable bytes separately from WAL and transient bytes;
- raw query samples and consolidated p50/p95/p99;
- ordered result IDs and digests before and after reopen;
- intended-route proof and explicit no-fallback/no-timeout state;
- typed per-case `unsupported` rows where a semantic is genuinely unavailable.

`lexical_common.validate_result` deterministically rejects document count or
content drift, missing/extra query cases, duplicates, IDs outside the corpus,
reference ID/order mismatch, digest mismatch, missing reopen proof, missing
intended-route evidence, fallback, timeout, malformed unavailable rows, and
sample-count drift. Unsupported rows stay in the equivalence ledger and never
enter the headline matrix.

Consolidation emits `treedb_lexical_comparison/v1` and fails unless an accepted
TreeDB text-v2 row and accepted equivalent rows from at least two external
engines survive all retained repetitions.

## Artifacts

A direct run writes:

- `lexical_comparison.json`: machine-readable consolidated build, storage,
  latency, raw-sample, result, context, and equivalence data;
- `lexical_comparison.md`: headline, build/storage, availability/equivalence,
  command, version, and configuration matrices;
- `corpus.tsv`: exact consumed corpus bytes;
- `raw/<engine>-r<repetition>.json`: shared-schema engine artifacts;
- `logs/<engine>-setup.log` and `logs/<engine>-r<repetition>.log`: exact command
  capture and bounded output;
- optional `indexes/` only with `--keep-indexes`.

The combined script places these under `$RUN_DIR/lexical/`; its pre-existing
hybrid artifacts remain `$RUN_DIR/scoreboard.json`, `$RUN_DIR/scoreboard.md`,
TreeDB Go benchmark logs, and `$RUN_DIR/context.txt`.

The old single-query SQLite `--docs/--queries/--query` path and static
Lucene/Tantivy/Bleve unavailable placeholders are intentionally removed. They
did not share a corpus or query manifest and must not be compared to the #4330
rows.

## Focused checks

These are the narrow checks for this comparator; they do not run the project
suite:

```sh
python3 benchmarks/text_hybrid_scoreboard/test_lexical_comparison.py

GOWORK=off go test ./benchmarks/text_hybrid_scoreboard/treedb_adapter

cd benchmarks/text_hybrid_scoreboard/bleve_adapter \
  && GOWORK=off go test ./...

cd benchmarks/text_hybrid_scoreboard/lucene_adapter \
  && mvn -q -DskipTests compile
```

Then run the retained command at the top of this document. There are no timing
threshold tests; correctness is established by manifest interpretation,
normalization, exact result validation, typed unsupported handling, route proof,
and reopen proof.
