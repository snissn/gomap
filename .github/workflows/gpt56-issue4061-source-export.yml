name: GPT-5.6 issue 4061 exact source export

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  export-source:
    if: github.head_ref == 'gpt56/issue-4061-bson-index-codec'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 1
      - name: Package exact branch source
        run: |
          set -euo pipefail
          tar --exclude=.git -czf /tmp/gomap-issue4061-source.tgz .
      - uses: actions/upload-artifact@v4
        with:
          name: gomap-issue4061-source-${{ github.sha }}
          path: /tmp/gomap-issue4061-source.tgz
          if-no-files-found: error
          retention-days: 1
